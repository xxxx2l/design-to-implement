# 父任务状态：{任务名}

> 本任务是**父任务**(任务拆分模式 A)。父任务负责总体设计 + 模块拆分 + 模块间接口契约 + 编排,不写模块内部实现。子任务在 `子任务/` 子目录下各自走完整 4 阶段流程。
>
> 这是本父任务的**记忆锚点**。任何新对话进入时**先读这个文件**,再读当前阶段对应的文档。

## 基本信息

- **任务一句话简介**:(这个父任务要解决什么问题,一两句话)
- **任务文件夹**:`docs/design/{序号}-{任务名}/`
- **创建时间**:YYYY-MM-DD
- **任务类型**:父任务(A 模式)

## 当前阶段

<!-- 父任务的阶段取值:讨论 / 设计(含模块拆分与接口契约) / 子任务执行中 / 集成测试 / 完成 / 暂停 -->

**当前阶段**:讨论
**下一步要做什么**:(一句话动作,如"和用户确认模块拆分方案")

## 子任务清单

<!-- 父任务的核心区块之一。模块拆分确定后维护。
依赖关系决定启动顺序:被依赖的子任务必须先完成,依赖方才能启动。 -->

| 序号 | 子任务 | 状态 | 依赖 | 文件夹 |
|------|--------|------|------|--------|
| 01-1 | (示例)敏感词过滤 | 未启动 | 无 | `子任务/01-1-敏感词过滤/` |
| 01-2 | (示例)人工审核工作台 | 未启动 | 01-1 | `子任务/01-2-人工审核工作台/` |

**状态取值**:未启动 / 讨论中 / 设计中 / 实施中 / 测试中 / 已完成 / 暂停

## 关键决策摘要

<!-- 3-5 条,防止重新讨论已定事项。重点是模块拆分和接口契约层面的决策 -->

- (示例)拆分为 3 个子任务:敏感词过滤、图片审核、人工审核工作台
- (示例)模块间通信用异步消息,不直接 HTTP 调用

## 待办与疑问

- [ ] (下一步要解决的具体问题)

## 文档索引

| 文档 | 路径 | 状态 |
|------|------|------|
| 沟通过程 | `01-沟通过程.md` | 进行中 |
| 设计 | `02-设计.md` | 未开始(待产出模块拆分 + 接口契约) |
| 实施(父级编排) | `03-实施.md` | 未开始(仅跨子任务集成,不写模块内部) |
| 测试(跨模块集成) | `04-测试.md` | 未开始(仅跨模块集成测试) |

## 上下文切换日志

| 时间 | 做到了 | 下次从哪继续 |
|------|--------|-------------|
| YYYY-MM-DD | (示例)确认了 3 个模块的拆分 | (示例)下次定模块间接口契约 |

## 变更记录

<!-- 父任务层面的方案变更。子任务内部的变更不在这里记,记在各子任务的状态文档。
父任务接口契约的变更会影响所有子任务,必须慎重并通知所有相关子任务。 -->

| 时间 | 类型 | 触发 | 内容摘要 | 产物影响 |
|------|------|------|---------|---------|
| (示例) | 【修改】 | 子任务 01-2 实施中发现接口契约字段缺失 | 父设计文档接口契约补充字段,通知所有子任务 | 依赖该接口的子任务需同步修订 |
