# 设计文档：{任务名}

> 本任务方案的**唯一事实来源**。实施文档只能反映本文档的内容。
>
> **方案发生变化，改的是本文件（含"设计变更记录"区块），而不是实施文档。**

## 1. 背景与目标

- **要解决的问题**：
- **业务目标**：
- **非目标**（明确不做的事）：

## 2. 整体方案

<!-- 一段话或一张图说清楚整体设计。先给结论，再展开细节。 -->

## 3. 详细设计

### 3.1 模块划分

### 3.2 核心流程

### 3.3 数据模型

### 3.4 关键算法/规则

## 4. 技术选型

| 决策点 | 选型 | 理由 | 被否的备选 |
|--------|------|------|-----------|
| （示例）鉴权 | JWT | 无状态，易扩展 | Session（扩展麻烦） |

## 5. 接口变动清单（后端项目必填）

<!-- 纯后端项目专属。任何新增或变动的接口都集中在这里，给前端直接联调用。
没变动的接口不必列。新增标【新增】，变动标【变动】。
接口清单在设计阶段必须给出确定结论，禁止把判断推到实施阶段。
凡是"新增还是变动""路径沿用还是改"等依赖现有代码才能定论的事实，必须在设计阶段读现有代码确认。
"设计阶段只讨论不写代码"针对的是业务代码——为确认接口形态去读既有代码属于设计调研，不违反约束。
禁止写"具体取决于现有代码""实施阶段再确认"，也禁止用【实施期确认】标签把不确定性合法化。
交付前端时接口清单必须完全确定。 -->

### 【新增】POST /api/xxx

- **鉴权要求**：需要 JWT / 无需
- **请求体**：
```json
{
  "field1": "string",
  "field2": 0
}
```
- **成功响应**（200）：
```json
{
  "code": 0,
  "data": {}
}
```
- **错误码表**：

| code | 含义 |
|------|------|
| 0 | 成功 |
| 40001 | 参数错误 |
| 40101 | 未授权 |

- **备注**：

### 【变动】GET /api/yyy

- **变动点**：（示例）新增返回字段 `newField`，旧字段 `oldField` 标记废弃，3 个版本后移除
- **当前请求体**：（同原接口，省略可写"无变动"）
- **当前响应**：（只写变动部分）
- **备注**：

## 6. 影响范围

<!-- 本方案会改动哪些现有模块、是否有破坏性变更、是否需要数据迁移 -->

## 7. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|------|------|------|------|

## 8. 验收标准

<!-- 需求边界：每个功能点"做到什么程度算满足需求"。这是规则层面（实例层面的用例在 04-测试.md）。
写法：用功能点 + 可判定的成功条件，不写具体输入输出（那是用例的事）。
验收标准是"做完了"的客观定义，实施期验收以此为依据。 -->

| 功能点 | 验收标准（可判定） |
|--------|------------------|
| （示例）高危词拦截 | 命中高危标签的文章：不落库、返回 40901、不暴露具体词 |
| （示例）中低危转审核 | 命中中低危标签的文章：正常落库为 pending_review、投人工审核队列、接口返回 reviewStatus=pending_review |
| （示例）第三方超时降级 | 阿里云 1500ms 无响应：文章降级放行（degraded=true）、投异步复核队列、触发告警 |

## 9. 设计变更记录

<!-- 本文件的演进史。和"沟通过程文档"互补——沟通过程是流水账，这里是结论的变更史。
每次方案有重要变动（哪怕是实施期才发现的问题）都记一行。 -->

| 时间 | 版本 | 变更内容 | 变更原因 |
|------|------|---------|---------|
| YYYY-MM-DD | v1.0 | 初稿 | 讨论阶段结论固化 |
