# 实施文档：{任务名}

> 把 `02-设计.md` 拆成可执行、可勾选的步骤清单。本文档是"如何做"，设计文档是"做什么、为什么"。
>
> **不要在本文档夹带方案变更**——遇到与设计不一致，停下来，先改 `02-设计.md`（含变更记录），再回来同步修订本文档。

## 标记规范

**小步骤四态标记**：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞（后方注明原因）

**大步骤状态符号**（放在标题末尾，带进度计数 `已完成/总数`）：

- `⬜ 0/N` 未开始
- `🌗 x/N` 进行中（部分完成）
- `✅ N/N` 全部完成

## 前置准备

- [ ] 拉取最新代码，确认分支
- [ ] 阅读完毕 `02-设计.md`

## 实施步骤

### 1. {大步骤名} ⬜ 0/3

- [ ] 1.1 （具体到可执行的动作，如"创建 users 表迁移脚本"；完成后对照用例 T-XX-XX 验证）
- [ ] 1.2
- [ ] 1.3

### 2. {大步骤名} ⬜ 0/2

- [ ] 2.1
- [ ] 2.2

### 3. 全量回归 ⬜ 0/2

- [ ] 3.1 跑回归用例集（T-R），确保局部改动未引发全局回归
- [ ] 3.2 跑接口用例（T-API），对照设计文档"接口变动清单"逐个验证

## 实施期备忘

<!-- 实施过程中的零散记录：踩到的坑、临时决策、待回写设计的发现。
重要变更不要只写在这里，必须同步到 02-设计.md 的"设计变更记录"。

⚠️ 发现方案缺口（漏字段/分支/字典值等）时的硬性约束：
- 立即停下当前实施，不要直接改实施文档继续做
- 先更新 02-设计.md（补设计变更记录）→ 等用户确认 → 再修订本实施文档 → 继续
- 这是"修改机制"，详见 SKILL.md
- 若发现的是根本性错误（方案方向不对），改为建议回退，由用户最终判定 -->

- （示例）发现 article_safety 缺一个 checked_by 字段 → 已走修改机制：先在 02-设计.md v1.x 补字段并记变更记录，等用户确认后回到步骤 1.1 补迁移

## 完成确认

- [ ] 所有步骤已完成（全 ✅）
- [ ] `04-测试.md` 中所有功能用例（T-F）、异常用例（T-EX）、接口用例（T-API）通过
- [ ] 回归用例集（T-R）最近一次重跑通过
- [ ] 已更新 `00-任务状态.md` 和 `docs/design/README.md` 标注完成
